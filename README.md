What this repo is (numerical methods course programs; languages used).

How to run things (Python env, Fortran build notes, LAPACK requirement, etc.).

A table of contents linking to each topic folder.

Any global conventions (naming, data files live in /data, plotting libs, etc.).